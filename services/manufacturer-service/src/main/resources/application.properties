# Spring Profiles
spring.profiles.active=dev
server.port=8081
spring.application.name=manufacturer-service

# Supabase PostgreSQL Configuration - FINAL VERSION
# prepareThreshold=0 là giải pháp tối ưu nhất cho prepared statement caching
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.grgbbhzjlddgocgyhekd
spring.datasource.password=Abc@123456!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration for PostgreSQL - FINAL VERSION
# DDL: Dùng 'update' nếu muốn giữ lại dữ liệu, 'create-drop' chỉ dùng cho DEV/TEST.
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# FIX: Cài đặt bắt buộc để tránh xung đột với Pooler
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=true

# Connection pool settings for Supabase
# Pool size nhỏ là chuẩn khi dùng Pooler bên ngoài
spring.datasource.hikari.maximum-pool-size=3 
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.idle-timeout=600000

# Health Check
management.endpoints.web.exposure.include=health,info,metrics
management.health.db.enabled=true
management.endpoint.health.show-details=always

# Logging for debugging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE